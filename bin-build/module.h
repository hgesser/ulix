#line 246 "../bachelorarbeit.nw"
/* Header-Datei */

/* Diese Funktion wird aus der Initialisierung des
   Betriebssystems heraus aufgerufen. */
void initialize_module ();
extern int printf(const char *format, ...);
